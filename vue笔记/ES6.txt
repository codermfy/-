使用let声明的变量可以重新赋值,但是不能在同一作用域内重新声明 块级作用域 没有变量提升 
使用const声明的变量必须赋值初始化,但是不能在同一作用域类重新声明也无法重新赋值

模板字面量 链接字符串 ${}

解构赋值

对象字面量简写法

for...of循环 可以循环任何可迭代（也就是遵守可迭代协议）类型的数据 
默认情况下，包含以下数据类型：String、Array、Map 和 Set
默认情况下，对象不可迭代。
（for...in 循环循环访问所有可枚举的属性，意味着如果向数组的原型中添加任何其他属性）

展开运算符（用三个连续的点 (...) 表示）
一个用途是结合数组。

剩余参数(可变参数)

ES6箭头函数
对于箭头函数,this的值基于函数周围的上下文, 换句话说,this的值和函数外面的this的值是一样的.

默认参数函数

类
super extends